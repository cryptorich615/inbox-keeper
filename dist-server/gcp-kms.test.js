import { describe, expect, it, vi } from 'vitest';
import { GoogleCloudKms } from './gcp-kms.js';
describe('GoogleCloudKms', () => { it('wraps and unwraps through the SDK client', async () => { const client = { encrypt: vi.fn(async () => [{ ciphertext: Buffer.from('wrapped') }]), decrypt: vi.fn(async () => [{ plaintext: Buffer.from('plain') }]) }, kms = new GoogleCloudKms('projects/p/locations/l/keyRings/r/cryptoKeys/k', client); expect(await kms.wrap(Buffer.from('plain'))).toBe(Buffer.from('wrapped').toString('base64url')); expect(Buffer.from(await kms.unwrap(Buffer.from('wrapped').toString('base64url'))).toString()).toBe('plain'); expect(client.encrypt).toHaveBeenCalledOnce(); expect(client.decrypt).toHaveBeenCalledOnce(); }); it('rejects incomplete key names', () => expect(() => new GoogleCloudKms('key', {})).toThrow()); });
