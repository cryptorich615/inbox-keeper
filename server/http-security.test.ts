import{describe,expect,it}from'vitest';import{requireMutation,sessionCookie}from'./http-security.js';
describe('HTTP security',()=>{it('sets opaque HttpOnly host cookie',()=>{expect(sessionCookie('opaque',true)).toContain('__Host-inbox_session=opaque');expect(sessionCookie('opaque',true)).toContain('HttpOnly');expect(sessionCookie('opaque',true)).toContain('SameSite=Lax');expect(sessionCookie('opaque',true)).toContain('Secure')});it('uses a non-__Host cookie on plain localhost so browsers do not reject it',()=>{const cookie=sessionCookie('opaque',false);expect(cookie).toContain('inbox_session=opaque');expect(cookie).not.toContain('__Host-');expect(cookie).not.toContain('Secure')});it('rejects missing Origin',()=>{const req={headers:{'x-csrf-token':'csrf'}}as any;expect(()=>requireMutation(req,{id:'s',userId:'u',csrf:'csrf',expiresAt:1,oauthState:null,pkceVerifier:null},'https://app.test')).toThrow(/origin/)})});
